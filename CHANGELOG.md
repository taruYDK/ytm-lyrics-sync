# 変更履歴

## v2.7.14-beta.1（公開ベータ1）

- Music Glassの背景・テーマを統合。ポップアップからON／OFFを切り替えられます。
- ジャケットの縁とサイズを調整し、スクロールバーを非表示にする設定を追加しました。
- 開発中のbeta.2〜beta.8をまとめた初回公開版です。内部バージョンは2.7.14を維持します。

## v2.7.14-beta.8

- ポップアップに「再生画面のスクロールバーを隠す」を追加。スクロール操作はそのまま利用できます。背景テーマがOFFでも利用できます。

## v2.7.13-beta.7

- Music Glassのジャケットを広い画面では6%、中程度の画面では3%拡大。小さい画面では従来のサイズを維持します。

## v2.7.12-beta.6

- 背景・テーマのスイッチを右側に配置し、ON／OFFの文字と色で状態を判別できるようにしました。
- スイッチが白い丸だけになる表示崩れを修正しました。

## v2.7.11-beta.5

- ポップアップにMusic Glassの直接ON/OFFスイッチを追加。詳細設定と同じ保存値を利用。

## v2.7.10-beta.4

- 保存通知と「元に戻す」の表示時間を60秒から5秒に短縮。通知の幅は変更なし。

## v2.7.9-beta.3

- ジャケットの影を完全に削除し、内側の画像コンテナの枠・影・角丸も解除。外枠一か所だけで角丸を適用。

## v2.7.8-beta.2

- ジャケットの内側の縁取りを除去し、影を弱めました。画像と外枠の二重の角丸を整理。実画面での見た目は未確認。

## v2.7.7-beta.1

- Music Glass v1.2.7のテーマと専用設定画面を統合。
- 設定名を分離し、歌詞機能との衝突を防止。

新しいバージョンから順に掲載しています。

## v2.7.6

- 歌詞欄のツールバーが狭い場合は取得状態を省略し、操作ボタンを優先。
- その他メニューを歌詞パネル内の高さに制限し、縦スクロールに対応。
- 狭い画面の再同期・補正を「再生・同期」に分類。

## v2.7.5

- 幅1100px以下では再同期・歌詞補正を「その他」へ移動し、再生バーの音量スライダーとの重なりを避ける配置に変更。広い画面では再生バーへ戻ります。

## v2.7.4

- 現在行への即時復帰がCSSのスムーズスクロールを引き継ぐ問題を修正。復帰時はinstantを指定し、手動スクロール待機への再突入を防止。

## v2.7.3

- 歌詞欄の上端に短いフェードを追加し、スクロール時の文字の見切れを軽減。歌詞の配置・追跡位置は変更なし。

## v2.7.2

- 取得状態を歌詞候補・その他と同じ行に移動し、専用行の高さを解消。長い提供元名は省略表示し、マウスを重ねると全文を表示。

## v2.7.1

- 同期歌詞や歌詞タブが見つからない場合のログを情報レベルに変更。通信・解析エラーの警告は維持。歌詞の取得条件は変更していません。

## v2.7.0 操作と状態表示の改善

- 歌詞の検索中・取得済み・同期歌詞なしを提供元とともに表示。
- 手動スクロール後の待機中に「今の歌詞に戻る」を表示。
- ふりがな編集に、実際の配置処理を使う入力プレビューを追加。
- 歌詞編集・手動ふりがな・同期補正の保存後60秒間、「元に戻す」で直前の変更を取り消し。他タブで更新された内容は上書きしません。曲切り替え・ページや拡張機能の再読み込み後は利用できません。
- その他メニューを「歌詞を探す」「編集する」「保存・診断」に整理。無効な項目の理由を表示。


## v2.6.7

- YouTube Music提供元のHTTPエラー、タイムアウト、応答形式などの失敗理由をコンソールに記録。同じ理由は1分間抑制し、歌詞・認証情報・曲IDは記録しません。
- 配布生成前にビルド一致と全自動テストを必須化。失敗した場合は配布生成を中止します。

## v2.6.6

- 保存曲の表示メニューとポップアップの選択メニューを共通デザインに統一。余白・角丸・選択表示を改善。
- 対応ブラウザーでは展開した候補も配色・余白を統一。旧ブラウザーは標準メニューを維持。

## v2.6.5

- 拡張機能本体を extension/、説明資料を docs/ に整理。
- 配布物から開発用ソース・テスト・辞書再生成用データを除外。
- 歌詞取得や再生操作の機能変更はありません。

## v2.6.4

- 歌詞行にフォーカスがあるとき、Spaceで再生・一時停止。クリックとEnterはその位置へ移動します。
- Spaceの長押しによる連続切り替えを防止しました。

## v2.6.3 YouTube Music本体の行同期歌詞

YouTube Musicの動画IDから歌詞タブを取得し、時刻付き歌詞を候補に追加します。提供元設定でON/OFF可能。認証情報・Cookieは使わず、同一サイトへ匿名で問い合わせます。全時刻ゼロの歌詞や通常テキストだけの歌詞は同期候補にしません。曲切り替え後の応答は破棄します。

指定曲 nBs26EgzsS0 は匿名API検証で29行の有効な同期歌詞を確認。自動テスト70件合格。拡張機能を読み込んだ実ブラウザー画面での表示は未確認。認証が必要な歌詞や別音源の探索は未対応です。

API形式の調査参考：https://github.com/naikaku1/YTM_Immersion/blob/main/src/js/module/ytm-lyrics.js （公開実装を参照し、取得・解析処理は本拡張用に実装）。

## v2.6.2 処理負荷の軽減

- 日本語辞書をoffscreen document内の共有Workerへ移動。ページ上で辞書を展開せず、タブ間で共有。5分間未使用でWorkerを終了して辞書を解放。外部へ歌詞は送信しません。
- 手動読みの移行済みフラグを保存し、移行後のストレージ全体読み込みを停止。通常の応答は対象曲の読みだけです（保存マップ自体はサービスワーカーで読み込み）。
- 歌詞キャッシュの永続化を1秒でデバウンス。メモリ上のキャッシュは即更新。
- プレイヤー通信は約30Hz、メタデータは250ms間隔。拡張機能OFFでポーリング停止。
- offscreen権限を追加。Chrome 111以上の既存要件は維持。
- 全68件の自動テスト合格。実ブラウザーのメモリ量・停止時間は未測定。

設計参考：https://developer.chrome.com/docs/extensions/reference/api/offscreen

## v2.6.1 検索待ち時間の改善

LRCLIBの曲名・アーティスト指定検索を既存検索と並行開始。使用可能な歌詞は全提供元の終了を待たずキャッシュへ保存します。曲の一致判定・旧曲の結果を捨てる処理は維持。自動テスト62件合格。実際の通信時間は未測定で、提供元の混雑や認証待ちは残ります。

## v2.6.0 メニュー順序

「その他」のふりがな編集を「歌詞を追加」の直下へ移動。

## v2.5.9 歌詞クリック後の追跡復帰

曲IDが一致する歌詞クリックでシークした場合、手動スクロール待機と曲開始ガードを解除して追跡を再開。再生位置は実プレイヤーの値を使用します。全61件のテスト合格。修正版の実サイト確認は未実施。

## v2.5.8 読み込み直後の追跡

歌詞の初回表示では現在行へ即座に位置合わせ。非表示の歌詞欄は表示後に再配置し、ふりがな辞書の初期化より先に本文の追跡位置を更新します。手動スクロール中の待機は維持。自動テスト60件合格。実サイトでの確認は未実施です。

## v2.5.7 ふりがなの追跡順序

同期単語内の本文・ふりがな・かなに、文字位置に応じた個別の進捗区間を割り当てました。各rubyが同時に進む問題と親の重複描画を修正。「なめらか」「ぬるぬる」双方に適用。既存の同期時刻は変更せず、区間内は文字数に応じて配分します。自動テスト59件合格。修正版の実サイト再生確認は未実施です。

## v2.5.6 読み仮名の修正と保存管理

- 本文rbにも単語の追跡グラデーションを明示し、親要素のクリップに依存しない描画に変更。
- 未知英単語の同一綴りルビを抑制。英字と漢字の隣接時は本文・辞書の読みを境界判定に利用。
- 手動読みを分割してから日本語・英語の表示設定を適用。
- 手動読みを共通の曲別保存領域へ移行し、一覧・曲削除・バックアップ復元・通知件数に追加。旧キーは新形式の保存成功後に削除。
- 新たな保存では本文と同一本文の出現順を識別に使い、他の行の挿入で位置がずれても対応。旧データの一意な本文も追従。本文の変更や同一行の繰り返し回数変更は完全な照合を保証しない。
- cmudict.dict.gzは辞書再生成の再現性のため同梱を維持。
- 自動テスト58件合格。実機確認はComputer UseがブラウザーURLを確認できず停止したため未実施。

## v2.5.5 ふりがな編集の有効状態

歌詞なし・曲の読み込み未完了・表示曲の不一致・拡張機能無効時は「その他」のふりがな編集を無効化。曲と歌詞が使用可能になると再び有効にします。実サイトでの操作確認は未実施です。

## v2.5.4 手動ふりがなの配置修正

行全体の手動読みをひとつのrubyにして中央へ固めていた表示を修正。送り仮名と空白を手がかりに漢字・英単語ごとへ分割して配置します。既存の保存データにも自動適用され、行単位の編集と追跡を維持します。本文と読みの送り仮名や空白が一致しない場合は文字数で分配するため、位置が近似になる場合があります。画像の歌詞・英単語・同期断片・非表示を含む全55件のテストとビルド一致を確認。実サイトの表示は未確認です。

## v2.5.3 行ごとのふりがな編集

「その他」→「ふりがな編集」に移動しました。行を選ぶと現在の読みが入力済みで表示され、行全体を書き直して「この行を保存」で反映できます。対象文字の入力は不要です。同じ本文が繰り返されても選択した行だけを変更します。空欄で保存するとその行の読みを非表示にできます。旧版の部分修正は引き継がれます。行全体の手動読みは本文の文字数に応じて単語へ配分して追跡します。実サイトでの操作確認は未実施です。

## v2.5.2 手動のふりがな編集

歌詞パネルの「ふりがな編集」で行・対象の文字・読みを指定して保存できます。漢字と英語に対応し、単語追跡にも反映します。同一曲の同じ本文の行には共通で適用されます。空の読みで非表示にでき、「この行を自動読みに戻す」で解除できます。本文や同期時刻は変更しません。修正はこのブラウザーのローカル領域に曲ごとに保存され、現在のバックアップ対象には含まれません。別のブラウザーへの同期はありません。実際のYouTube Music画面での操作確認は未実施です。

## v2.5.1
- 漢字のふりがな・英語のカタカナ読みも本文と同じ単語進捗で色が進むようにした。なめらか・ぬるぬるの両方に対応。
- 行同期では現在行の読みを明るく表示。追跡OFFでは読みを固定色に戻す。
- 既存の進捗変数をCSSで引き継ぎ、毎フレームの追加処理は行わない。

## v2.5.0
- 漢字のひらがなと英語のカタカナ読みを追加。設定から個別にON/OFFでき、バックアップにも保存。
- 日本語解析ライブラリーと日英辞書を同梱し、読みの生成は端末内で実行。
- 元の歌詞とタイミングを保ったままrubyを表示。辞書読み込み中の曲切り替え・設定変更では古い結果を破棄。
- 英語は約12万語を収録した辞書から近似カタカナに変換。未知語には無理に読みを付けない。

## v2.4.6
- 文字選択の抑止対象を、歌詞本文・メニュー・補正・設定画面・バックアップ画面を含む拡張UI全体とその子要素へ拡大。
- 入力欄・テキストエリア・編集可能な文字だけは、選択・コピー・貼り付けを維持。

## v2.4.5
- 「その他」や歌詞候補、補正画面などの追加UIで、連続クリックによる文字の選択（青い表示）を抑止。
- 入力欄・テキストエリア・編集可能な領域は、従来通り文字選択とコピー・貼り付けが可能。歌詞本文や明示的なコピーボタンの機能は変更しない。

## v2.4.4
- 同期歌詞作成モードのショートカット除外を作成パネル内の操作ボタン・リンクに限定。
- パネル外の再生ボタンにフォーカスがある場合もSpaceで記録、Ctrl+Zで取り消しが可能。パネル内の保存・戻る等は本来のキー操作を維持し、記録ボタンは例外。
- パネル内外のボタン・リンク・role=buttonを分けた回帰テストに更新。全44件のテストに合格。
- 実機確認はComputer Useが現在URLを判定できず停止したため未実施。

## v2.4.3
- 壊れていたwhite-space宣言6か所を修正。
- 補正スライダーはinputでプレビュー、changeで保存。フォーカス中は同じ曲のスライダーDOMを作り直さない。
- 追跡OFFの毎フレームDOMクリアを停止。
- 補正画面のEscape・フォーカス復帰と、遅く／早くの端ラベルを追加。
- 保存通知と警告を2.2秒で消去（保存失敗は残す）。提供元設定も保存失敗を判定。
- 設定コントロールの読み上げ名、歌詞行のEnter/Spaceシークを追加。作成モードは保存ボタン等のSpaceを横取りしない。
- 復元した文字サイズ・待ち時間が標準の選択肢にない場合も、その値を表示。
- Better Lyricsの認証・JWT・Cookieと設定同期についてPRIVACYを更新。
- デバウンスの対象をREADMEで明確化。minimum_chrome_versionを111に設定。

## v2.4.2
- 追跡設定の保存を500ミリ秒のデバウンスでまとめ、ポップアップが非表示になるときは保留中の値を送信。
- 曲全体の補正が±20秒の上限に達した方向はボタンを無効化し、上限を表示。
- 通信タイムアウトを1〜60秒に制限。不正値は25秒。
- バックアップの設定・曲別補正・固定歌詞・ローカル歌詞・編集内容を、書き込み前に検証。復元要求は保存側でも検証。
- READMEを更新し、旧FIXESとRELEASE文書をこの変更履歴へ統合。
- 背景通信テストのバージョンをmanifestから取得。

## v2.4.1
- 簡単な同期調整を保存側で直列に加減算し、連打や複数タブでの古い値による上書きを防止。
- 保存された補正を各タブに反映。保存失敗を表示。

## v2.4.0
- 追跡位置を上から25〜65%で調整（標準42%）。
- 手動スクロール後の復帰時間を1・2・3.5・5・10秒から選択（標準3.5秒）。
- 曲全体の歌詞を0.1秒ずつ早く／遅くする操作を追加。途中補正の差は維持。
- 歌詞領域のサイズ変更後に現在行を設定位置へ合わせる。手動閲覧中は復帰を待つ。
- 新しい設定もバックアップに含める。

## v2.3.6
- 追跡位置を中央から上から42%へ変更。

## v2.3.5
- 冒頭の余白を従来の位置へ戻し、終盤用の余白を維持。

## v2.3.4
- 曲の冒頭・終盤でも中央まで移動できる余白を追加（冒頭はv2.3.5で元に戻した）。

## v2.3.3
- 複数時刻タグや時刻順でない拡張LRCで、単語が別の行に付く問題を修正。
- 単語にもoffsetを適用し、100分以上のタグに対応。不正な単語時刻を除外。
- LRC書き出し時の時刻なし行・null行・0秒行への負の補正を修正。

## v2.3.2 の修正

- テーマ関連の属性とOSの明暗設定の変更を監視し、次の描画フレームで配色を再判定します。1秒間隔のチェックは検知漏れを補うために残しています。
- 主要ボタンの背景・ホバー・文字色をCSS変数へ集約しました。配色は従来どおりです。

## v2.3.1 の配色・操作性改善

- 歌詞パネルはYouTube Music側の背景色から明暗を判定します。判定できない場合はテーマ属性・OS設定を参照します。設定・バックアップ画面はOSの明暗設定に対応します。
- 主要な配色をCSS変数に集約し、ライト背景での文字・入力欄・メニュー・バッジの配色を調整しました。
- キーボード操作時のフォーカス枠を統一しました。設定のスイッチにもTabキーで移動できます。
- 中評価バッジの文字と背景のコントラストを調整しました。
- パネルを開くときの短いアニメーションと、動きを減らす設定への対応を追加しました。閉じる処理は別タブへの表示残りを防ぐため即時です。
- 進捗バーの形・背景・塗りを明示しました。Firefox用の見た目指定もありますが、拡張機能自体のFirefox対応を保証するものではありません。

## v2.3.0 の使いやすさ改善

- 同期歌詞作成中に **Spaceで記録して次へ／Ctrl+Zで1行戻す** 操作ができます。入力欄・別の曲・歌詞画面以外では作動しません。長押しによる連続記録を防ぎます。
- 作成モードに記録済み行数の進捗バーを追加しました。
- オフセット調整は従来どおり即時反映され、補正パネルにも現在行をプレビューします。
- 「データ保護 → お気に入り・編集履歴を開く」から保存曲に戻れます。固定歌詞をお気に入りとして扱い、保存・編集の更新順／お気に入り／曲名順で表示します。再生履歴の収集は行いません。
- 前回のバックアップ書き出し以降に保存・更新された曲が20曲以上になると、ポップアップとバックアップ画面に通知します。同じ曲の連続編集は1曲として数えます。ファイルの自動保存は行いません。書き出し後、ファイルの保存を確認した場合に通知件数をリセットします。
- 歌詞候補のおすすめ度を高（85以上：緑）・中（70以上：黄）・低（70未満：グレー）で色分けしました。点数と説明も表示します。
- 設定画面に文字サイズ・フォーカス・追跡アニメーションのサンプルを追加しました。変更を即時確認できます。

## v2.2.2 の更新（v2.2.1 の変更を含む）

- 標準の歌詞タブが無効な曲でも、「歌詞」から外部歌詞の表示・検索画面を開けるようにしました。
- 外部歌詞画面の固定背景をなくし、YouTube Musicの背景・タブの書体や文字サイズに馴染む表示にしました。
- 次のコンテンツ・コメント・関連コンテンツへの切り替え、またはEscapeキーで外部歌詞画面を閉じられます。
- 外部提供元にも歌詞がない場合は、手動検索やローカル歌詞の追加をご利用ください。スマホ版の歌詞を直接取得する機能ではありません。

更新前に設定のバックアップを保存し、既存の拡張フォルダーへ上書きしてください。拡張機能とYouTube Musicのページを再読み込みすると反映されます。

## v2.2.1 補足
- 標準歌詞がない曲の外部歌詞タブと、他タブ・Escapeで閉じる操作を追加。v2.2.2へ継承。

## v2.2.0 の保守性・安全性改善

- `content.js` を直接編集する構成から、`src/content/` の16個の機能別ソースをビルドする構成に変更しました。配布用の `content.js` は生成物として引き続き同梱します。
- タイミング補間とLRC書き出しを、`src/core/` の独立したES Modulesに切り出しました。
- バックグラウンド通信で追加できるヘッダーを `Content-Type` のみに制限し、現在使っているJSON・フォーム形式だけを許可しました。送信元の拡張機能IDも確認します。
- `tests/` に計算・LRC出力・ヘッダー制限・保存競合・ビルド結果一致のユニットテストを追加しました。ZIPにもソース・ビルドスクリプト・テストを含めています。

開発手順は [DEVELOPMENT.md](DEVELOPMENT.md) を参照してください。Node.js 20以降で `npm run build` と `npm test` が実行できます。外部パッケージのインストールは不要です。拡張機能を使用するだけならNode.jsは必要ありません。

保存キー・拡張機能ID・既存のUIは維持しています。更新後は拡張機能とYouTube Musicの全タブを再読み込みしてください。

## v2.1.2 の修正

- 関連コンテンツ・コメント・次のコンテンツで歌詞UIが残る問題を修正しました。
- 選択中のタブと表示領域の種類を確認し、歌詞タブ以外では追加UIと標準歌詞の置き換えを無効にします。
- 歌詞タブへ戻ると、取得済みの歌詞を再表示します。保存データや歌詞検索処理は変更していません。

## v2.1.1 の表示整理

- 歌詞上部は「歌詞候補」「その他」の2つに整理しました。
- 手動検索・歌詞編集・歌詞を追加・LRC保存は「その他」から開けます。
- 曲ID・提供元・同期方式などは「その他 → 診断情報」で確認・コピーできます。
- メニューは再クリック・外側のクリック・Escキーでも閉じられます。
- 歌詞の検索・保存・同期処理は変更していません。

## v2.1.0 の追加・改善

- **保存の競合対策**：固定・解除・ローカル歌詞保存をバックグラウンドで順番に処理します。複数タブからの操作にも対応し、補正・歌詞編集・手動検索・管理画面の削除・復元も同じ保存経路を使用します。固定処理中は候補パネルのボタンを操作不可にします。
- **更新後の案内**：拡張機能との接続が無効になった場合、検索の再試行を止め、YouTube Musicのページを再読み込みする案内とボタンを表示します。
- **LRC保存**：歌詞タブの「LRC保存」から現在の歌詞をダウンロードできます。保存済みの書き換え・行削除を反映し、曲ごとのタイミング補正を含めるか選択できます。
- **保存データ管理**：ポップアップの「データ保護」に保存曲数・使用量を表示します。「バックアップ・復元・管理を開く」から曲名・アーティスト・曲IDで検索し、不要な曲のデータを確認付きで削除できます。
- **歌詞編集の保存失敗対策**：保存完了を待ってから画面を閉じます。保存失敗時は入力内容を残し、エラーを表示します。

### LRC保存について

- 出力は行同期の通常LRCです。単語・音節単位の同期情報は含みません。
- タイミング補正の反映は初期状態ではOFFです。ONでは、各補正ポイントから歌詞の表示開始時刻を計算します。極端な補正で同じ行が複数回現れる場合、その繰り返し動作までは再現しません。
- 補正を反映したLRCを同じ曲へ再登録する場合、二重補正を避けるため曲の補正を0に戻してください。
- 作成モードの未保存の下書きは、先に保存してから書き出してください。
- LRCは端末内で生成します。LRCLIBへの直接投稿は行いません。公開・共有する場合は歌詞の権利に注意してください。

### 更新・削除時の注意

既存の拡張機能IDと保存キーは維持しています。念のためバックアップを作成し、拡張機能を削除せず、読み込み中のフォルダーへ新版のファイルをすべて上書きしてください。新しい `storage-broker.js` と `data-manager.js` も必要です。

拡張機能を再読み込みしたあと、**開いているYouTube Musicの全タブも再読み込み**してください。更新前の古いタブは新しい保存処理を使えません。

管理画面での削除は、選択した曲の補正・歌詞編集・固定歌詞・ローカル歌詞・手動検索条件が対象です。表示設定・通常歌詞キャッシュは残します。削除後の復元には事前のバックアップが必要です。削除・復元後はYouTube Musicを再読み込みしてください。

## v2.0.2 の修正・改善

### 曲切り替え時の不具合を修正

ローカル歌詞の保存や歌詞候補の固定・解除の処理中に曲が切り替わった場合、処理の完了によって新しい曲の歌詞検索や選択状態が変更されないよう修正しました。

### 保存データの自動削除を廃止

保存曲数が増えると、古いタイミング補正や歌詞編集が自動削除される処理を廃止しました。

これまでの上限は、タイミング補正が500曲、歌詞編集が250曲でした。今回から件数を理由に自動削除しません。

※ 過去に削除されたデータを復元する機能ではありません。

### 同期歌詞作成モードを軽量化

行を記録するたびに一覧全体を作り直していた処理を改善しました。表示要素を再利用し、長い歌詞の記録・取り消し操作で発生する描画負荷を軽減しています。

### 更新について

保存済みの補正・歌詞編集・固定候補・ローカル歌詞は引き継げます。拡張機能を削除せず、現在読み込んでいるフォルダーへ上書きし、拡張機能とYouTube Musicを再読み込みしてください。

## v2.0.0 のアップデート

### この曲ではこの歌詞を固定
- 「歌詞候補」の各候補にある「📌 この歌詞を固定」から、選んだ歌詞を動画IDごとに保存できます。
- 次回は通常キャッシュやネット検索より先に固定した歌詞を表示します。
- 固定を解除すると、その場で通常の候補検索へ戻ります。

### ローカル歌詞を追加
- 「歌詞を追加」から、LRCの貼り付けと `.lrc` ファイルの読み込みができます。
- 保存した歌詞は動画IDごとに端末内へ保存され、すべての外部提供元より優先されます。
- 保存済みのローカル歌詞は確認後に削除でき、削除すると固定候補または通常検索へ戻ります。

### 同期歌詞作成モード
- 時刻のない歌詞を1行ずつ貼り付け、曲を再生しながら「この行を記録して次へ」を押すとLRC時刻を作成できます。
- 「1行戻す」「記録をやり直す」「曲の先頭へ」に対応しています。
- 前の行より前の時刻や、途中で別の曲へ切り替わった状態では誤保存しません。

### 診断情報
- 歌詞画面に、状態・曲ID・提供元・同期方式・候補数を小さく表示します。
- 「診断情報をコピー」で、拡張機能の版、補正ポイント数、プレイヤー時刻の取得元などをコピーできます。
- 診断情報には歌詞本文や認証情報を含めません。

### 保存と優先順位
- 優先順位は `ローカル歌詞 → 固定した歌詞 → 通常キャッシュ → ネット検索` です。
- 固定歌詞とローカル歌詞は、従来の補正・歌詞編集と同様にバックアップ／復元できます。
- 固定歌詞とローカル歌詞は件数で自動削除せず、保存に失敗した場合は入力画面を閉じずにエラーを表示します。
- v1.9.11までに保存した補正・検索条件・歌詞編集はそのまま引き継がれます。

## v1.9.11 の新機能
- 歌詞編集で、不要な歌詞行を表示から削除できるようにしました。
- 削除したい行にカーソルを置いて「選択行を削除」を押すと、複数行でも削除対象にできます。
- 削除した行の設定は書き換えと同様に曲・歌詞候補ごとに保存され、バックアップ／復元の対象になります。
- 残した歌詞行の同期タイミングは維持されます。

## v1.9.10 の改善
- 曲ごとの歌詞タイミング補正範囲を、±10秒から±20秒へ広げました。
- これまで保存した曲ごとの補正値は、そのまま引き継がれます。

## v1.9.9 の新機能
- 歌詞上部の「歌詞編集」から、現在表示している歌詞の文章を書き換えられます。
- 各行のタイミングは維持され、変更した行だけ単語・音節同期から行同期へ切り替わります。
- 書き換えは動画IDと歌詞候補ごとに自動保存され、次回再生時にも反映されます。
- 「元の歌詞に戻す」で、その候補に保存した書き換えを削除できます。
- 書き換えた歌詞もバックアップ／復元の対象に追加しました。

## v1.9.8 の修正（ここから更新しています）
- 曲名が同じでもアーティストが一致しない検索結果は、自動採用しないようにしました。
- 英字アーティスト名は「姓・名」の語順違いや共同アーティスト表記を考慮します。
- 再生時間が大きく異なる候補も拒否します。
- 過去に保存された誤った歌詞を再表示しないよう、歌詞キャッシュを更新しました。

## v1.9.7 の改善
- ZIPを別フォルダへ展開しても同じ拡張機能として認識されるよう、拡張機能IDを固定しました。
- ポップアップの「データ保護」から、曲ごとの補正値・手動検索内容・表示設定をJSONファイルへバックアップ／復元できます。
- 歌詞キャッシュやBetter Lyricsの認証情報はバックアップへ含めません。

> v1.9.6以前から初めてv1.9.7へ移る時だけ、固定IDの導入により従来の保存領域とは別になります。現在の補正を残したい場合は、同梱の移行用v1.9.6.1で先にバックアップし、v1.9.7で復元してください。v1.9.7以降は通常の更新で補正値を維持できます。

## v1.9.6 の改善
- 「すべての補正を削除」を押した時に確認ダイアログを表示します。
- 「キャンセル」では設定を維持し、「OK」を押した場合だけ補正を削除します。

## v1.9.5 の修正
- LRCLIBの曲名中心の高速検索を先に行い、YouTube側のアーティスト表記がチャンネル名でも同期歌詞を見つけやすくしました。
- キャッシュ歌詞が曲ID更新中に適用できなかった場合、検索まで終了して空表示になる問題を修正しました。
- 曲切替直後に古い曲IDが短時間残っても、現在のURLまたはPlayer情報が一致すれば取得済み歌詞を反映するようにしました。
- タイミング補正画面の軽量化と、補正欄を1つから追加する方式は維持しています。

## v1.9.4 の改善
- 補正欄は最初に「曲の最初」の1つだけ表示し、必要な位置へ追加していく方式に変更しました。
- 最後に追加したポイント以降は、そのポイントの補正値をそのまま維持します。
- 再生中に300msごとに全ポイントを作り直していた処理を廃止し、補正画面とYouTube Musicの動作を軽量化しました。
- ポイント一覧は追加・削除・値の確定など、内容が変わった時だけ再描画します。

## v1.9.3 の新機能
- 曲の好きな再生位置へ歌詞タイミングの「補正ポイント」を何個でも追加できるようにしました。
- 各ポイントは個別に±0.1秒・±0.5秒またはスライダーで調整でき、途中のポイントだけ削除できます。
- ポイント間の補正量は滑らかに変化し、設定は曲ごとに自動保存されます。
- v1.9.2以前の「曲の最初・曲の最後」の補正値はそのまま引き継がれます。

### 好きな位置からずれを合わせる方法
1. ずれが気になる場所まで曲を再生します。
2. 下部プレイヤーの「歌詞 ±0.0秒」を開き、「現在位置に補正ポイントを追加」を押します。
3. 追加された時刻の補正値を調整します。別の場所でも同じ操作を繰り返せます。

## v1.9.2 の改善
- 「歌詞候補」または「手動検索」をもう一度押すと、開いているパネルを閉じられるようにしました。
- 別のボタンを押した場合は、パネルを閉じずに内容を切り替えます。

## v1.9.1 の新機能
- 歌詞候補ごとに「おすすめ度 0〜100」と「最もおすすめ / おすすめ / 良好 / 候補 / 要確認」を表示します。
- おすすめ度は同期精度、曲の言語との一致、設定した提供元の優先順位、歌詞行数から自動計算します。ユーザー投票や外部評価ではありません。

## v1.9.0 の新機能
- 歌詞タブ上部の「歌詞候補」から、取得済みの同期歌詞を提供元・同期精度・行数を見ながら選択できます。
- 「手動検索」から曲名とアーティスト名を修正して再検索できます。手動条件は動画IDごとに保存され、自動検出へ戻すこともできます。
- ポップアップで Better Lyrics、LRCLIB、Unison、BiniLyrics、Karalyr の有効/無効と優先順位を変更できます。

## v1.8.7 の新機能
- 曲の最初と最後で異なるタイミング補正を設定できるようにしました。
- 2つの補正値は再生位置に合わせて滑らかに補間されるため、曲が進むほどずれが大きくなる同期歌詞にも対応できます。
- 設定はこれまでどおり曲ごとに保存されます。v1.8.6以前の一律補正値は、最初・最後の両方へ自動的に引き継がれます。

### 位置によってずれる曲の合わせ方
1. 下部プレイヤーの「歌詞 ±0.0秒」をクリックします。
2. 曲の序盤を再生しながら「曲の最初」を調整します。
3. 曲の終盤へ移動し、「曲の最後」を調整します。
4. 途中の補正量は自動計算されます。＋で歌詞が早く、−で遅く表示されます。

## v1.8.6 の修正
- 同期歌詞候補が取得済みなのに言語推定だけで全候補が除外され、「見つかりません」になる問題を修正しました。
- YouTube Music標準歌詞から言語を判定できた場合は、タイトルやアーティスト名の文字種より本文言語を優先します。
- 歌詞タブ検索を実際のタブコンテナ内へ限定し、タイミング補正ボタンなどの誤クリックを防ぎました。
- 拡張機能をOFFにした時は追加ボタンとプレイヤー幅変更も無効になります。
- 複数タイムタグを持つLRCの単語同期割り当てを修正しました。
- 自動表示設定の説明を実際の「最後の操作から2秒」に合わせました。

## v1.8.5 の修正
- 「次のコンテンツ」表示後、約1.9秒無操作なら歌詞タブへ移動します。
- YouTube Music側が1回目の自動クリックを取りこぼしても、実際に歌詞へ切り替わるまで短時間再試行します。
- 歌詞タブ検出を強化し、`role=tab` / `tp-yt-paper-tab` / `yt-formatted-string` など現在UIの複数構造に対応します。
- 待機開始**前**のクリックやスクロールはキャンセル扱いにしません。待機開始**後**に操作した場合だけ、その曲の自動移動をキャンセルします。
- 設定「無操作時に歌詞を自動表示」のON/OFFを維持します。
