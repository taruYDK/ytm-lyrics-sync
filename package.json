{
  "name": "ytm-lyrics-sync",
  "version": "2.5.6",
  "private": true,
  "scripts": {
    "build": "node scripts/build.cjs",
    "check:build": "node scripts/build.cjs --check",
    "test": "node --test",
    "check": "npm run check:build && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
