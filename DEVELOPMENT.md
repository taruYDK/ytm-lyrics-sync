# 開発・テスト

Node.js 20以降が必要です。ビルドとユニットテストに外部パッケージは不要です。

```sh
npm run build
npm test
npm run check:build
```

`content.js` は生成物です。直接編集せず、`src/` を変更してからビルドしてください。生成済みファイルを同梱しているので、拡張機能を利用するだけならNode.jsは不要です。

## 構造

- `src/core/timing.mjs`：純粋なタイミング補間関数。単位はミリ秒、位置は0〜1。
- `src/core/lrc.mjs`：LRC書き出しと補正時刻の逆算。入力の歌詞・補正データを変更しません。
- `src/content/lyrics-panel.js`：歌詞候補、編集、ローカル歌詞作成、メニュー、診断表示。
- `src/content/timing-offset.js`：曲別の補正ポイント管理と操作画面。
- `src/content/auto-open.js`：歌詞タブ判定、自動表示、タブ操作。
- `src/content/providers.js`：外部提供元への問い合わせ。
- `src/content/lyrics-storage.js`：保存した歌詞、編集、キャッシュ、表示への適用。
- `src/content/track-search.js` / `tracking.js`：曲切り替え時の検索と同期追跡。
- `src/content-order.json`：ビルドする機能別ソースの順番。
- `scripts/build.cjs`：依存パッケージなしのビルド。構文を検査し、単一の隔離スコープ内へ結合します。
- `tests/`：Node標準テストランナーで実行する、コア計算・送信制限・保存競合・生成物一致のテスト。

コアは直接importできるES Modulesです。UI側は既存動作を維持するため、共有状態を持つビルド用ソースとして機能別に分割しています。UIファイル間の依存関係まで完全に独立化した構成ではありません。次の段階で必要に応じて状態の受け渡しを明示的にしていけます。

コアモジュールはビルド時に関数のexport宣言を外して組み込みます。任意のESM構文を処理する汎用バンドラーではありません。新しいimportやクラスを追加する場合はビルド側も対応させてください。ビルドの構文チェックと生成物一致テストが不整合を検出します。

## テスト対象と制限

タイミングの境界・補間・±20秒、LRC丸め、行削除・編集、補正反映の逆算、ヘッダーの許可リスト、送信元とホスト制限、保存処理の直列化などを検証します。合成データだけを使い、ネットワーク送信や利用者の保存データの削除は行いません。

ライブのYouTube Musicと外部APIの変更はユニットテストだけでは確認できません。変更後はテスト用プロファイルで「歌詞→関連→歌詞」の往復、候補固定、ローカル歌詞、LRC保存、拡張更新後の再読み込み案内も確認してください。

## v2.4.2の検証

全37件の自動テストが成功。連続設定変更のまとめ保存、ポップアップ終了時の保留値送信、補正上限の表示、通信タイムアウト、バックアップの中身の検証と不正復元時の既存データ維持を追加確認しています。
backup-validation.jsはバックアップ画面とサービスワーカーの両方から読み込みます。新しい配布ファイル一式を更新してください。

## v2.4.3の検証

全44件の自動テストに合格。CSS宣言、補正スライダーのプレビューと保存分離、フォーカス中の再描画抑止、追跡OFF時のDOM操作回数、作成モードのボタン保護、歌詞行のキーボード操作、復元値の選択肢、通知の消去と保存エラー表示を追加確認しました。
実機操作を試みましたが、Computer UseがブラウザーURLを確実に判定できず停止したため、実機でのドラッグ継続・見た目は未確認です。

## v2.4.4の検証

作成パネル内外のボタン・リンクを分けたショートカット回帰テストへ更新し、全44件の自動テストに合格。再生ボタンクリック後のSpace記録と「1行戻す」クリック後のCtrl+Zは、Computer UseのブラウザーURL判定エラーで操作が停止したため実機未確認です。

## v2.5.0の検証

全49件の自動テストに合格。実際の同梱日本語辞書12ファイルを使った解析、英語辞書の読み、送り仮名、時刻付き断片への読み分割、曲変更時の非同期結果破棄、元データ・単語DOMの維持とOFF時の復帰を確認しました。
実際のYouTube Music画面での表示確認は未実施です。読み仮名は19番目の機能別ソース群の一部で、ビルド対象は19機能・3コアモジュールです。vendorフォルダーも更新時に必要です。
英語辞書はnode scripts/build-readings.mjsで、同梱の元データから再生成できます。日本語辞書・ライブラリーの出典と変更点はvendor/README.mdを参照してください。

## v2.5.1の検証

読み仮名のrt要素が親の単語進捗変数を引き継ぐCSSへ変更。既存49件のテストとビルド一致を確認。実サイトでの表示確認は未実施です。

## v2.5.2 手動のふりがな編集

歌詞パネルの「ふりがな編集」で行・対象の文字・読みを指定して保存できます。漢字と英語に対応し、単語追跡にも反映します。同一曲の同じ本文の行には共通で適用されます。空の読みで非表示にでき、「この行を自動読みに戻す」で解除できます。本文や同期時刻は変更しません。修正はこのブラウザーのローカル領域に曲ごとに保存され、現在のバックアップ対象には含まれません。別のブラウザーへの同期はありません。実際のYouTube Music画面での操作確認は未実施です。

検証結果：npm run check（ビルド一致・全52件）合格。手動読みの上書き・非表示・同期断片の本文維持・別曲の分離・保存読み込みエラーを検証。

## v2.5.3 行ごとのふりがな編集

「その他」→「ふりがな編集」に移動しました。行を選ぶと現在の読みが入力済みで表示され、行全体を書き直して「この行を保存」で反映できます。対象文字の入力は不要です。同じ本文が繰り返されても選択した行だけを変更します。空欄で保存するとその行の読みを非表示にできます。旧版の部分修正は引き継がれます。行全体の手動読みは本文の文字数に応じて単語へ配分して追跡します。実サイトでの操作確認は未実施です。

検証：npm run check のビルド一致・全53件合格。行番号による独立編集と旧形式の互換性を確認。

## v2.5.4 手動ふりがなの配置修正

行全体の手動読みをひとつのrubyにして中央へ固めていた表示を修正。送り仮名と空白を手がかりに漢字・英単語ごとへ分割して配置します。既存の保存データにも自動適用され、行単位の編集と追跡を維持します。本文と読みの送り仮名や空白が一致しない場合は文字数で分配するため、位置が近似になる場合があります。画像の歌詞・英単語・同期断片・非表示を含む全55件のテストとビルド一致を確認。実サイトの表示は未確認です。

## v2.5.5 ふりがな編集の有効状態

歌詞なし・曲の読み込み未完了・表示曲の不一致・拡張機能無効時は「その他」のふりがな編集を無効化。曲と歌詞が使用可能になると再び有効にします。実サイトでの操作確認は未実施です。

## v2.5.6 読み仮名の修正と保存管理

- 本文rbにも単語の追跡グラデーションを明示し、親要素のクリップに依存しない描画に変更。
- 未知英単語の同一綴りルビを抑制。英字と漢字の隣接時は本文・辞書の読みを境界判定に利用。
- 手動読みを分割してから日本語・英語の表示設定を適用。
- 手動読みを共通の曲別保存領域へ移行し、一覧・曲削除・バックアップ復元・通知件数に追加。旧キーは新形式の保存成功後に削除。
- 新たな保存では本文と同一本文の出現順を識別に使い、他の行の挿入で位置がずれても対応。旧データの一意な本文も追従。本文の変更や同一行の繰り返し回数変更は完全な照合を保証しない。
- cmudict.dict.gzは辞書再生成の再現性のため同梱を維持。
- 自動テスト58件合格。実機確認はComputer UseがブラウザーURLを確認できず停止したため未実施。


## v2.5.7 ふりがなの追跡順序

本文・ふりがな・かなに文字位置に応じた個別の進捗区間を割り当て、複数箇所が同時に進む問題を修正。なめらか・ぬるぬる両対応。区間内は文字数による配分です。自動テスト59件合格。修正版の実サイト再生確認は未実施です。


## v2.5.8 読み込み直後の追跡

初回表示は現在行へ即座に移動し、非表示中の位置合わせは表示後に再試行。ふりがな辞書初期化の前に本文を追跡します。手動スクロール待機は維持。全60件のテスト合格、実サイト確認は未実施。


## v2.5.9 歌詞クリック後の追跡復帰

曲ID一致の歌詞クリックでシークすると、手動スクロール待機と曲開始ガードを解除。実プレイヤー時刻に追従します。全61件合格。修正版の実サイト確認は未実施。


## v2.6.0 メニュー順序

ふりがな編集を「歌詞を追加」の直下へ移動。全61件テスト合格。


## v2.6.1 検索待ち時間の改善

LRCLIB指定検索を並行開始。採用済み歌詞は全検索完了を待たずキャッシュ保存。自動テスト62件合格、実通信時間は未測定。

## v2.6.2 処理負荷の軽減

- 日本語辞書をoffscreen document内の共有Workerへ移動。ページ上で辞書を展開せず、タブ間で共有。5分間未使用でWorkerを終了して辞書を解放。外部へ歌詞は送信しません。
- 手動読みの移行済みフラグを保存し、移行後のストレージ全体読み込みを停止。通常の応答は対象曲の読みだけです（保存マップ自体はサービスワーカーで読み込み）。
- 歌詞キャッシュの永続化を1秒でデバウンス。メモリ上のキャッシュは即更新。
- プレイヤー通信は約30Hz、メタデータは250ms間隔。拡張機能OFFでポーリング停止。
- offscreen権限を追加。Chrome 111以上の既存要件は維持。
- 全68件の自動テスト合格。実ブラウザーのメモリ量・停止時間は未測定。

設計参考：https://developer.chrome.com/docs/extensions/reference/api/offscreen

